services:
  - type: web
    name: ai-model
    repo: https://github.com/Capstone-Tech4Dev/AI-model
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars: []